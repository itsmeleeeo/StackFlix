import React from 'react' 
import './Database.css'

function DatabaseTitle(props) {
    return(
        <div>
            <h1 className="dba">{props.text}</h1>
        </div>
    )
}

export default DatabaseTitle